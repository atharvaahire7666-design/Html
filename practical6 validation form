<!DOCTYPE html>
<html>
    <head>
        <title> validation form</title>
        <script>
            function data()
            let Name=document.getElementById("name").value;
            let contect=document.getElementId("contect").value
            let password=document.getElementId("password").value
            let confirm password=document.getElementId("confirm password").value
            
            if(name==""||contect=""||password=""||confirm password=="")
        {
             alter("Allfield are mendatory")
             return false;

        }
        </script>
        <form onsubmit=" return data()">
            userid: <input type="text" id="name"><br><br>
            contect:<input type="number" id="contect"><br><br>
            password:<input type="password" id="password"><br><br>
            confirm password:<input type="password" id="password"><br><br>
            <input type="submit" value="submit"><br><br>
        </form>

    </head>
</html>
